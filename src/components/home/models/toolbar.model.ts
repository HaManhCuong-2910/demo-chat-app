export enum EPositionToolbar {
  left = "left",
  right = "end",
  center = "center",
}

export enum EMode {
  dark = "dark",
  light = "light",
}

export enum EUserInterface {
  ios = "ios",
  android = "android",
}
